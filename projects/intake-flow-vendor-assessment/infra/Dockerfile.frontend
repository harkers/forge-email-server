FROM nginx:alpine
# Build on host and copy dist directly — avoids Docker-layer build cache issues
COPY frontend/dist /usr/share/nginx/html
COPY infra/nginx.frontend.conf /etc/nginx/conf.d/default.conf
EXPOSE 3000
CMD ["nginx", "-g", "daemon off;"]
