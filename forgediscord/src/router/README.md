# Router Module

Responsibilities:
- resolve trust boundary
- evaluate route config
- choose workspace/workflow/agent
- apply fallback handling when ambiguous
- ensure allowed tools + memory scopes remain explicit
