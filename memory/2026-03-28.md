# 2026-03-28 Memory Log

## DevForge: Project Foundry Planning Started

Created dedicated planning workspace:
`/home/stu/.openclaw/workspace/workspaces/devforge-project-foundry`

Key decisions:
- This is the master OpenClaw workspace and future GitHub template repo for governed project delivery.
- Future repo name: `devforge-project-foundry`.
- Internal domain naming should use `forge-*` consistently.
- ForgeComms, ForgeTraining, and ForgeRisk are dedicated lifecycle domains, not side features.
- Privacy has its own dedicated role.
- Current status is planning only; no build/bootstrap should begin before planning gates are approved.

Planning docs created under:
`/home/stu/.openclaw/workspace/workspaces/devforge-project-foundry/docs`

## Quarto Launcher Fixed

Quarto itself was installed and working, but the bundled Deno was being overridden by system Deno. Fixed by ensuring Quarto uses its bundled Deno:
- Bundled Deno: `~/.local/quarto/bin/tools/x86_64/deno` (v2.3.1)
- System Deno was at `/home/stu/.deno/bin/deno` (v2.7.7)
- Solution: Use bundled Deno for Quarto operations

## Environment Health Checks

From heartbeat checks:
- **Down:** Forge Pipeline API (18103), Usage Dashboard (8899), Phi-4-mini (8091), Coder-7B (8092)
- **OK:** Quarto, Ollama, ZFS, disk space

## Workspace Configuration Improvements

Added planning vs delivery separation rule to:
- Root `AGENTS.md`
- `docs/workspace-configuration-improvement-plan.md`
- Project Foundry docs (`PHASED_PLAN.md`, `GATES.md`, `REVIEW_PACK.md`)

Created canonical workspace registry:
- `REGISTRY.md` at workspace root
- Inventoried 35 top-level directories with status classification
- Noted ambiguity flags for ForgeOrchestra naming collision and other cleanup candidates

Added root-level policy rules to `AGENTS.md`:
- Human governance vs AI operating roles (after Red Lines)
- Minimum serious-project pack (after Quarto defaults)
- Memory promotion rule (within Memory section)
- DevForge Project Foundry authority (within Forge Workspace Defaults)

Fixed usage dashboard cost-state handling:
- Token reporting and cost reporting now tracked separately
- Sessions with real tokens but unreliable zero-cost now show `usageState: "reported"` and `costState: "unknown"`
- No longer pretending fake-zero cost is known spend

## Phase 5: Environment Health Checklist

Created `HEARTBEAT.md` with environment health checklist:
- Quick checks: Quarto, Forge Pipeline API, Usage Dashboard, local model lanes, memory search
- Extended checks: paired devices, ZFS health, Docker containers, disk space
- Tracking guidance in `memory/heartbeat-state.json`
- Remediation notes for common failures

## Registry Ambiguity Resolution

Investigated and resolved ambiguity flags:
- **ForgeOrchestra collision**: `forgeorchestrate` is canonical active workspace; `forge-orchestra` and `forgeorchestra` are earlier iterations/design docs
- **trilium directory**: Empty placeholder; active config is in `sso-home-lab/trilium/`
- **projects substructure**: Documented three sub-projects separately
- **control-plane-what-next-v2-extracted**: Cleanup candidate for archive/removal

Updated `REGISTRY.md` with resolutions and refined status classification.

## Titan Dashboard (Homepage) Planning

Created `/home/stu/.openclaw/workspace/projects/titan-dashboard/`:

- README.md with architecture, quick start, groups
- deploy/docker-compose.yml with Homepage + docker-socket-proxy
- deploy/config/settings.yaml with global _blank target and layout
- deploy/config/docker.yaml for Docker integration via proxy
- deploy/config/services.yaml for manual overrides
- deploy/config/widgets.yaml for system widgets
- deploy/config/custom.css for 125×125 launcher tiles
- deploy/config/bookmarks.yaml (empty, for future)
- deploy/LABELS.md with label standard documentation
- deploy/nginx/homepage.conf for Nginx reverse proxy

Architecture:
- Homepage as launcher UI with Docker label discovery
- docker-socket-proxy for secure read-only Docker API access
- Nginx as reverse proxy at dash.titan.local
- All links open in new tab (_blank)
- Custom CSS enforces 125×125 tile grid

Label standard defined for auto-registration:
- homepage.group, homepage.name, homepage.icon, homepage.href, homepage.description
- optional: homepage.weight, homepage.widget.*

Groups:
- Core: OpenClaw, Homepage, Nginx, Authentik, Trilium
- Infra: Proxmox, Portainer, Grafana, Uptime Kuma
- Forge: Forge Pipeline, ForgeHome, ForgeGarden
- Labs: Ollama, Stable Diffusion, test apps

## RAG System Already Exists in MCP Control Plane

**Critical discovery:** The MCP Control Plane at `/data/appdata/mcp-control-plane` already has a RAG stack:

- **Profile:** `--profile rag`
- **Container:** `openclaw-embedding-worker`
- **Port:** Internal 8080
- **Gateway label:** `embedding-worker`
- **Vector DB:** Qdrant
- **Tools:**
  - `ingest_document` — fetch URL/file → chunk → embed → Qdrant
  - `search_documents` — semantic similarity search

**Location:** `/data/appdata/mcp-control-plane/services/embedding-worker/`

**Status:** Not currently running (profile not started)

**Memory gap identified:** I didn't know this existed because:
1. No memory entry for MCP Control Plane RAG stack
2. TOOLS.md doesn't reference it
3. REGISTRY.md doesn't list it under active services

This should have been the first thing I checked when asked about RAG. Adding it to memory now.

## pfSense Nexus Controller Documentation

Ingested pfSense Nexus Controller setup docs from Netgate:
- `docs/reference/pfsense-nexus-controller-setup.md`
- Enable controller: System > Advanced > Netgate Nexus tab
- Firewall rules: VPN port (UDP) on WAN, GUI access restricted
- Authentication: Uses same credentials as pfSense Plus WebGUI