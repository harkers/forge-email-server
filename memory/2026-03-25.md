# 2026-03-25

## OpenClaw Cron CLI Improvements (Completed)

**Task:** Improve OpenClaw cron operator UX and safety

**Status:** ✅ Completed

**Implementation Location:** `/home/stu/openclaw-cron-fix/`

### What Was Implemented

**New CLI Commands:**
- `openclaw cron show <id-or-name>` - Inspect job details
- `openclaw cron trigger <id-or-name>` - Run once, preserve schedule
- `openclaw cron suspend <id-or-name> --for 2h` - Temp suspend with auto-resume
- `openclaw cron history [id-or-name]` - View run history

**Enhanced Existing Commands:**
- All commands now accept name or ID
- `list --state enabled|disabled|suspended` - Filter by state
- `list --tag <tag>` - Filter by tag
- Confirmation prompts show job summary before actions
- Ambiguous names display disambiguation

**Schema Changes:**
- Added `suspendedUntil` to `CronJobState`
- Added `tags` to `CronJob`
- Added `CronSuspendParamsSchema`, `CronTriggerParamsSchema`
- Updated `CronEvent.action` with `"suspended"` and `"enabled"`

**Files Modified:**
- `src/cli/cron-cli/register.ts`
- `src/cli/cron-cli/register.cron-simple.ts`
- `src/cli/cron-cli/shared.ts`
- `src/cron/service/state.ts`
- `src/gateway/protocol/schema/types.ts`
- `src/gateway/protocol/schema/protocol-schemas.ts`
- `src/gateway/protocol/index.ts`
- `src/cli/cron-cli.test.ts`

**New Files:**
- `src/cli/cron-cli/register.cron-show.ts`
- `src/cli/cron-cli/register.cron-trigger.ts`
- `src/cli/cron-cli/register.cron-suspend.ts`
- `src/cli/cron-cli/register.cron-history.ts`

**Tests:** 42/42 passing
**Build:** Succeeds

**Forge Pipeline:** Added to `openclaw-control-ui` project, status: completed

---

## Session Notes

- Morning: Control-plane testing for ForgeOrchestrate
- Afternoon: Completed cron CLI improvements task