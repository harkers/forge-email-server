# 2026-03-25

## OpenClaw Cron CLI Improvements (Completed)

**Task:** Improve OpenClaw cron operator UX and safety

**Status:** ✅ Completed

**Implementation Location:** `/home/stu/openclaw-cron-fix/`

### What Was Implemented

**New CLI Commands:**
- `openclaw cron show <id-or-name>` - Inspect job details
- `openclaw cron trigger <id-or-name>` - Run once, preserve schedule
- `openclaw cron suspend <id-or-name> --for 2h` - Temp suspend with auto-resume
- `openclaw cron history [id-or-name]` - View run history

**Enhanced Existing Commands:**
- All commands now accept name or ID
- `list --state enabled|disabled|suspended` - Filter by state
- `list --tag <tag>` - Filter by tag
- Confirmation prompts show job summary before actions
- Ambiguous names display disambiguation

**Schema Changes:**
- Added `suspendedUntil` to `CronJobState`
- Added `tags` to `CronJob`
- Added `CronSuspendParamsSchema`, `CronTriggerParamsSchema`
- Updated `CronEvent.action` with `"suspended"` and `"enabled"`

**Files Modified:**
- `src/cli/cron-cli/register.ts`
- `src/cli/cron-cli/register.cron-simple.ts`
- `src/cli/cron-cli/shared.ts`
- `src/cron/service/state.ts`
- `src/gateway/protocol/schema/types.ts`
- `src/gateway/protocol/schema/protocol-schemas.ts`
- `src/gateway/protocol/index.ts`
- `src/cli/cron-cli.test.ts`

**New Files:**
- `src/cli/cron-cli/register.cron-show.ts`
- `src/cli/cron-cli/register.cron-trigger.ts`
- `src/cli/cron-cli/register.cron-suspend.ts`
- `src/cli/cron-cli/register.cron-history.ts`

**Tests:** 42/42 passing
**Build:** Succeeds

**Forge Pipeline:** Added to `openclaw-control-ui` project, status: completed

---

## Afternoon: Forge Pipeline v1.1.0 (Completed)

**Task:** Implement Phase 1 tickets for Forge Pipeline

**Status:** ✅ Completed

### What Was Implemented

**FP-001: Critical Priority**
- Added `critical` to ALLOWED_PRIORITY (low/medium/high/critical)
- Task priority can now be set to "critical"

**FP-002: Risk State Enum**
- New `risk_state` field on tasks: none, watch, at-risk, critical
- Database auto-migration (ALTER TABLE ADD COLUMN)
- Full CRUD support in API

**FP-010: KPI Labels**
- Renamed "Open" → "Active tasks"
- Added "At Risk" metric
- Added "Blocked" metric  
- Added "Critical" task count

**FP-011: KPI Deltas**
- Compare current vs previous snapshot (5-min intervals)
- Visual +N/-N indicators on metric cards
- Snapshot stored in Redis with 5-min TTL

**FP-020: Metadata Chip Reduction**
- Mini-lists show only critical/high priority badges
- Only show blocked status, not all statuses
- Risk state badges for at-risk/critical items

**FP-030: Empty-State Improvement**
- Better visual hierarchy with icon and guidance
- Contextual help text for new users

**FP-003: Source Health Indicator**
- Shows refresh freshness (✓ healthy / ⏳ delayed / ⚠ stale)
- Visual indicator next to last update timestamp

### Bug Fixes
- Fixed invalid default status `'active'` → `'not-started'`
- Fixed all status validation errors on POST requests
- Fixed DEFAULT_DATA using invalid status

### Files Modified
- `forge-pipeline/api/server.py` - API changes
- `forge-pipeline/app/public/app.js` - UI logic
- `forge-pipeline/app/public/index.html` - HTML structure
- `forge-pipeline/app/public/styles.css` - Styling
- `forge-pipeline/VERSION.md` - Version bump
- `forge-pipeline/IMPLEMENTATION_PLAN.md` - Status update

**Deployed:** Docker containers rebuilt and running
**URL:** http://192.168.10.80:4174

---

## Evening: Forge Pipeline v1.2.0 - v1.5.0 (Completed)

**Task:** Continue Forge Pipeline development through Phases 2-5

**Status:** ✅ Completed

### Phase 2: Semantic Layout Upgrade (v1.2.0)
- FP-040: Rebalance kanban column widths (1.15/0.85/1.0/1.0)
- FP-041: Relative timestamps on Recently changed items (Xm/Xh/Xd ago)
- FP-021: Overdue visual state (red border, pulsing badge)
- FP-022: Due-soon state (≤7 days, orange highlight)
- FP-023: Stale detection (>7 days since update, faded opacity)
- FP-050: Risk state badges on task cards and editor dropdown

### Phase 3: Management Insight Layer (v1.3.0)
- FP-051: Insight strip (high-priority due, blockers, stale, at-risk counts)
- FP-052: Focus Now panel (recommended next action)
- FP-053: Stale-work detection logic
- FP-054: Source health awareness panel
- FP-080: Derived state rules in scoring logic

### Phase 4: PostgreSQL Migration (v1.4.0)
- FP-DB1-DB6: Added SQLAlchemy, psycopg2-binary, database.py
- Dual-mode database layer (SQLite/PostgreSQL)
- PostgresConnection wrapper for sqlite3-compatible API
- **Note:** PostgreSQL disabled - connection pool exhaustion needs fix
- Currently running on SQLite mode

### Phase 5: Filters & Interaction (v1.5.0)
- FP-060: Full filter bar (priority, risk, status, source, sort controls)
- FP-061: Task detail drawer (slide-out editor panel)
- FP-062: Quick actions menu on task cards (edit ✓ ×)
- FP-063: Sorting logic (priority asc/desc, due date, updated)
- FP-064: Density modes (comfortable/compact/tight) with toggle buttons

### Key Technical Decisions
- Connection handling wrapped in try/finally blocks
- PostgresConnection class wraps SQLAlchemy for sqlite3 compatibility
- Task cards now show quick action buttons on hover
- Drawer for focused task editing
- Density toggle affects padding and grid layout

### Files Modified
- `forge-pipeline/api/server.py` - API with try/finally connection handling
- `forge-pipeline/api/database.py` - New database abstraction layer
- `forge-pipeline/api/migrate.py` - Migration script
- `forge-pipeline/app/public/app.js` - Filter, sort, drawer logic
- `forge-pipeline/app/public/index.html` - Filter controls, drawer HTML
- `forge-pipeline/app/public/styles.css` - Density modes, drawer styles
- `forge-pipeline/docker-compose.yml` - PostgreSQL service (disabled)
- `forge-pipeline/requirements.txt` - Added SQLAlchemy, psycopg2-binary

**Total Releases Today:** 6 versions (v1.0.4 → v1.5.0)
**Live URL:** http://192.168.10.80:4174

---

## Pending Work

- Phase 6: Design System Alignment (ForgeOrchestra integration)
- PostgreSQL connection pool fix for production use
- Re-enable PostgreSQL in docker-compose once pool exhaustion is resolved