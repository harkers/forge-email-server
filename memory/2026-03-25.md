
---

## Additional durable notes from 2026-03-25 afternoon

### File serving default updated again

User clarified the preferred LAN-serving default is now:
```bash
python3 -m http.server 18081 --bind 0.0.0.0
```

Behavior to preserve:
- prefer port `18081`
- if busy, use the next available port
- bind to `0.0.0.0` so the host IP (for example `192.168.10.80`) works
- kill the server after about 1 hour

### Test results analysis artifacts created

Generated these files in workspace root:
- `control-plane-what-next-test-analysis.md`
- `control-plane-what-next-test-analysis.html`
- `control-plane-what-next-test-analysis.qmd`
- `control-plane-what-next-test-results-20260325-0955.zip`

Served them over HTTP on port 18081 for LAN access.

### Important limitation discovered in exported ZIP

The exported control-plane test ZIP is valid and complete for the files it claims to contain, but it does **not** include the deeper validation artifacts the user asked for later.

Missing from current ZIP/export set:
- `after-state.json`
- actual token usage per run
- retry count
- runtime
- explicit quarantine metadata
- explicit downstream blocked list
- window token totals before/after
- slot-consumption evidence

Conclusion: those fields were not captured by the original test runner/export pipeline. To provide them, the suite must be rerun with enhanced instrumentation rather than just re-zipping existing outputs.
